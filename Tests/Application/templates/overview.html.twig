{% extends "base.html.twig" %}

{% block content %}
    <h1>{{ content.title }}</h1>

    {{ content.article|raw }}

    {% if content.form %}
        <h2> Form </h2>
        {{ form(content.form) }}
    {% endif %}
{% endblock %}
