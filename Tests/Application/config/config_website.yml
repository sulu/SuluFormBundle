imports:
    - config.yml

framework:
    router: { resource: "%kernel.project_dir%/config/routing_website.yml" }

security:
    firewalls:
        main:
            pattern: ^/
            lazy: true
